{
  "name": "admin-panel",
  "version": "1.0.0",
  "description": "Permission-tiered admin panel — AdminPanel smart contract (Hardhat) + React/ethers v6 frontend.",
  "main": "hardhat.config.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "export-abi": "node scripts/exportAbi.js",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "grant-test-roles": "hardhat run scripts/grantTestRoles.js --network sepolia"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^17.4.2",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
